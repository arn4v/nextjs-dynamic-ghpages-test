{
  "name": "dynamic-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "deploy": "touch out/.nojekyll && gh-pages -d out -t true",
    "start": "next start"
  },
  "dependencies": {
    "next": "11.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
    "gh-pages": "^3.2.2"
  }
}
